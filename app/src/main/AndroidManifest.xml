<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.artur.bandsapp2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".TransitionActivity" />
        <activity android:name=".InitializeBasicInfoActivity" />
        <activity android:name=".ProfileViewActivity" />
        <activity android:name=".GenresSearchDialogActivity" />
        <activity android:name=".InstrumentsSearchDialogActivity" />
        <activity android:name=".SkillsSearchDialogActivity" />
        <activity android:name=".BandsSearchDialogActivity" />
        <activity android:name=".EditMenuActivity" />
        <activity android:name=".CoverFlowActivity" />
        <activity android:name=".AlbumsSearchDialogActivity"></activity>
    </application>

</manifest>